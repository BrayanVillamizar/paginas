import React from 'react';
import '../ChatbotIcon.css'; // Asegúrate de tener el archivo CSS para los estilos

const ChatbotIcon = () => {
  return (
    <div className="chatbot-icon">
      <i className="bi bi-chat-dots-fill"></i> {/* Usando Bootstrap Icons */}
    </div>
  );
};

export default ChatbotIcon;
